function particle = createParticle(windowSize,standardDev,delta,noiz,time)

    particle.windowSize = windowSize;
    particle.standardDev = standardDev;
    particle.delta = delta;
    particle.noiz = noiz;
    particle.time = time;

end